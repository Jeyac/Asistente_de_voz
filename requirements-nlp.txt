# Alias de compatibilidad: instala requirements.txt (incluye spaCy) + modelo español
-r requirements.txt

# Tras pip install, descargar el modelo (también se hace en Dockerfile para Render):
#   python -m spacy download es_core_news_sm
